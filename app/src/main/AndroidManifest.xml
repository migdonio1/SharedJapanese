<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="migdonio1.sharedjapanese">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".app.WordsListActivity" />
        <activity android:name=".app.WordCreateActivity" />
        <activity android:name=".app.QuizMenuActivity" />
        <activity android:name=".app.QuizActivity" />
        <activity android:name=".app.SitesListActivity" />
        <activity android:name=".app.SiteDetailActivity"></activity>
    </application>

</manifest>